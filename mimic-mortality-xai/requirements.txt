# Core data / ML stack — stable versions with wheel support
# Note: Excluding shap temporarily (numba/llvmlite build issues); will add in Stage 2
pandas==2.0.3
numpy==1.24.4
scikit-learn==1.3.2
xgboost==2.0.3
matplotlib==3.8.2
# shap==0.44.0  <- deferred due to numba/llvmlite compilation issues with Python 3.11

# Google Cloud
google-cloud-bigquery==3.14.1
db-dtypes==1.2.0
pyarrow==14.0.1

# Optional: for fairness analysis (not yet required, but nice to have)
# fairlearn==0.10.0
